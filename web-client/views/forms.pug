extends app-layout

block content
  form
    .form-group
      label(for='form-selector') Select form:
      select(class='form-control' id='form-selector')
        option First format
        option Second format
